{
  "name": "poc-mountebank",
  "scripts": {
    "mb:start": "npx mb start --allowInjection --loglevel debug --debug",
    "test": "jest --config ./jest.config.js --runInBand",
    "test:docker": "docker-compose up --build --attach-dependencies --abort-on-container-exit mountebank-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "jest": "^27.0.6",
    "jest-spec-reporter": "^1.0.17",
    "mountebank": "^2.4.0",
    "uuid": "^8.3.2"
  }
}
