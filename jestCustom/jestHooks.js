const { deleteAllImpostors } = require('../mocks/mounteBankOps');


beforeAll(async () => {});

beforeEach(async () => {});

afterEach(async () => {});

afterAll(async () => {
    await deleteAllImpostors();
});
